# Historical demography

## Applying an attention to DNA-sequences 

## How to run

### Train step 
```
python main.py --config=big_config.gin train
```
### Prediction step 

```
python main.py --config=big_config.gin predict --path=output/model.pkl.gz
```